spring:
  application:
    name: web

logging:
  file: ${spring.application.name}.log

server:
  port: 8000

eureka:
  client:
    serviceUrl:
      defaultZone: http://spring:1300/eureka/, http://summer:1301/eureka/



